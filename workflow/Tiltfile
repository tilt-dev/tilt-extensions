load('ext://uibutton', 'cmd_button')

cmds = [
        ['echo', 'hi'],
        ['echo', 'second step']
        ['echo', 'third step']
        ['echo', 'fourth step']
        ['echo', 'bye']
    ]

clear_cmd = ['echo', 'resetting...']

index = 0

def set_work_cmds():
    local(['write_workflow.sh', cmds...])

def set_clear_cmd():
    local(['write_reset.sh', clear_cmd...])

def play():
    local(['play.sh'])

def replay():
    local(['replay.sh'])

def rewind():
    local(['rewind.sh'])

